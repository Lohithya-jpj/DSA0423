import pandas as pd
from sklearn.cluster import KMeans

data = {
"CustomerID":[1,2,3,4,5],
"Amount":[200,500,150,800,300],
"Visits":[5,10,3,15,7]
}

df = pd.DataFrame(data)

X = df[["Amount","Visits"]]

kmeans = KMeans(n_clusters=2)
df["Cluster"] = kmeans.fit_predict(X)

print(df)
