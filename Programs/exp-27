import numpy as np
from scipy import stats

revenue = np.random.normal(100,20,100)

mean = np.mean(revenue)
se = stats.sem(revenue)

ci = stats.t.interval(0.95, len(revenue)-1, loc=mean, scale=se)

print("Mean Revenue:", mean)
print("95% Confidence Interval:", ci)
